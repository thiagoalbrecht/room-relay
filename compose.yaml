services:
  room-relay:
    build:
      context: .
      network: host
    ports:
      - "[::]:8080:8080"
    restart: unless-stopped
    environment:
      MAX_PAYLOAD_BYTES: 65536
      MESSAGES_PER_SECOND: 100
    networks:
      - relay

networks:
  relay:
    enable_ipv6: true
